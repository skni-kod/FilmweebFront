import React from "react";

const Footer: React.FC = () => {
    return (
        <footer className={"wrap-box"}>
            FooterComponent
        </footer>
    );
}

export default Footer;